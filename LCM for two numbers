#include <stdio.h>
void main()
{
 int a,b,Greater,size;
 printf("Enter value for two numbers:");
 scanf("%d%d",&a,&b);
 Greater=a>b?a:b;
 size=Greater;
 while(1)
 {
     if(Greater%a==0 && Greater%b==0)
      break;
     else
      Greater+=size;
 }
 printf("%d is the LCM",Greater);
}
