#include<stdio.h>
void main()
{
 int a,i,check = 0;
 printf("Enter a number:");
 scanf("%d",&a);
 for(i=2;i<a/2;++i)
 {
  if(a%i==0)
  check++;
 }
 if(check == 1)
  printf("%d is not a prime number",a);
 else
  printf("%d is a prime number",a);
}
