#include <stdio.h>
void main()
{
 int i,n,m;
 printf("Enter a number:");
 scanf("%d",&n);
 printf("Enter a number for range of multiplication table:");
 scanf("%d",&m);
 for(i=1;i<=m;i++)
 {
  printf("\n%d * %d = %d",n,i,(n*i));
 }
}
